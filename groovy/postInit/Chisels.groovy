/*import team.chisel.api.carving.CarvingUtils

CarvingUtils.getChiselRegistry().addGroup(CarvingUtils.getDefaultGroupFor(groupName)) //Adds an new Group

CarvingUtils.getChiselRegistry().addVariation(groupName, CarvingUtils.VariationFor(itemStack, 0)) //Adds an new variation to an Group

CarvingUtils.getChiselRegistry().removeGroup(groupName) //Removes an existing Group

CarvingUtils.getChiselRegistry().removeVariation(itemStack, groupName) //Removes an variation from an Groupimport team.chisel.api.carving.CarvingUtils;

CarvingUtils.getChiselRegistry().addGroup(CarvingUtils.getDefaultGroupFor(groupName)) //Adds an new Group

CarvingUtils.getChiselRegistry().addVariation(groupName, CarvingUtils.VariationFor(itemStack, 0)) //Adds an new variation to an Group

CarvingUtils.getChiselRegistry().removeGroup(groupName) //Removes an existing Group

CarvingUtils.getChiselRegistry().removeVariation(itemStack, groupName) //Removes an variation from an Group*/